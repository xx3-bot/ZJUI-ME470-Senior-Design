# Simulation Examples

These CSV files are historical/offline viewer inputs only. They are not runtime
inputs for the integrated program and should not be used to drive hardware.

Formal runs should generate a fresh trajectory from the current pick/place
targets through `主程序代码/main.py --run-target-sequence`.
